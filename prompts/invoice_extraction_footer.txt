返却形式: 原則ルートに 伝票種別, 伝票番号, 伝票日付, 取引先, 明細(配列)。日付キーは必ず "伝票日付" とする（"日付" というキー名は使わない）。明細各要素: 明細番号, 商品名, 数量, 単価, 金額。単一明細でも配列で1要素。

画像に伝票が縦に複数枚ある場合は、ルートを JSON 配列とし、**各要素は物理的に1枚の伝票のみ**（伝票種別・伝票番号・伝票日付・取引先・明細）。別伝票の品目を他の要素の明細に入れない。1枚のみのときは単一オブジェクトでも、要素1つの配列でもよい。不明は空文字。説明文は書かないこと。
