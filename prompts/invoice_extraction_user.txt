この伝票画像を読み取り、JSONのみを返してください。ルートに必ず "伝票種別": 1 または 2 を含めてください。画像に「納品伝票」と書いてあれば 1、「仕入伝票」と書いてあれば 2 です。

帳票1（伝票種別=1・納品伝票）のとき:
・伝票番号: 伝票NO：のあと（半角英字1桁+数字6桁-数字2桁）
・日付: 伝票日付：のあと。返すときは YYYY-MM-DD（例: 20260512→2026-05-12）
・取引先: 関東日本フード（株）
・明細は "明細" 配列。各要素: 明細番号(①②), 商品名, 数量, 単価, 金額（単位kg）
・商品名: ①②の直後の文字（数量の上を優先）{snap_hint}
・単価: 単価：のあと、合計金額は￥のあと

帳票2（伝票種別=2・仕入伝票）のとき:
・伝票番号: 必ず「仕入伝票」という文字の直下付近の6桁のみ（右上の日付6桁や他の6桁は使わない）
・伝票日付: 右上などの日付（YYMMDD）。返すときは YYYY-MM-DD（例: 260430→2026-04-30）
・取引先: 全農石川（固定）
・品目が複数行あるときは明細を行ごとに分ける（明細配列に複数要素）
・商品名: 品名・規格列の商品名{snap_hint}
・数量: 商品名から数えて5列目右の「数量」列の値。2〜3桁なら末尾1桁を小数（例: 123→12.3）
・単価: 「原単価」列の値。末尾2桁を小数部（例: 56400→564.00）
・金額（行の合計）: 「原単価」列のすぐ右隣の列の金額（原価金額列）。原価金額ラベルの下ではなく表の同じ行で拾う

返却形式: 原則ルートに 伝票種別, 伝票番号, 伝票日付, 取引先, 明細(配列)。日付キーは必ず "伝票日付" とする（"日付" というキー名は使わない）。明細各要素: 明細番号, 商品名, 数量, 単価, 金額。単一明細でも配列で1要素。

画像に伝票が縦に複数枚ある場合は、ルートを JSON 配列とし、各要素を1枚分のオブジェクト（伝票種別・伝票番号・伝票日付・取引先・明細）にすること。1枚のみのときは単一オブジェクトでも、要素1つの配列でもよい。不明は空文字。説明文は書かないこと。
{master_block}
